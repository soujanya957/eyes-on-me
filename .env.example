# One file per robot: copy to .env.<name> (git-ignored), e.g. .env.tusker,
# and pick it with `eyes-on-me --robot <name>`. A plain .env holding
# SPOT_ROBOT=<name> sets the default.
SPOT_IP=192.168.80.3
SPOT_USER=user
SPOT_PASS=
